import React, { useState } from 'react';
import Counter from './components/Counter';

export default function App() {
 
  return (
    <div>
      {/* Counter Example in which using redux increment, decrement , add async , add if odd add no  */}
       <Counter />
    </div>
  )
}
